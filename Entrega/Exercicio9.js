//Calculo Velocidade media

var distance = Number(prompt('Qual a distancia a ser percorrida, em km'));
var velocity = Number(prompt('Qual a velocidade media, em km? '));

var timeTravel = distance / velocity;
